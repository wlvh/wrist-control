# 腕控：当前目标为通用系统滚轮

- 用户已授权把表冠输入转换为公开 CGEvent 上下滚轮事件，遵循鼠标指针所在区域的正常分发规则。不得加入浏览器识别、应用白名单、目标锁定、网页正文判断、浏览器扩展、移动鼠标或抢焦点。
- 系统滚轮不得依赖腕控自己的窗口可见或最小化状态；切换普通应用不暂停。仅在设备认证、用户启用、发送事件权限和电脑可交互状态均满足时输出。保留暂停、锁屏停止、断线清空旧动作。
- 不使用私有接口，不伪装运动、音频或其他后台会话。发现和重连只在手表应用前台进行。
- 系统输入前必须验证设备身份；蓝牙连接、服务 UUID、会话号和系统开发配对都不能替代应用层认证。保持单对设备的最小实现，不引入账号、云端或手机中继。
- 开发签名使用被忽略的 `Config/Local.xcconfig` 或 Xcode 本地设置。不得提交账号、证书、私钥、描述文件或设备标识。
- 原始真机日志留在被忽略的 `evidence/private/`；公开证据只保留不含个人标识的结果摘要。任何发布都应检查完整 Git 历史。
- 试用时只运行一份电脑应用，确认操作的是已安装实例，避免把构建目录副本当成目标。
- 输入坐标、设备认证、重复/旧会话、过期动作和权限/锁屏规则变更，须请一个独立审查者检查；普通界面调整不必全面审计。
- 分清编译、自动测试、模拟器、真机输入与脱离调试器运行的证据。不拿两设备时间戳直接相减。
- 保留线性映射和小幅输入，不增加惯性、自动滚动或复杂加速度。验收使用真实浏览器和一个非浏览器应用，并脱离调试器运行；不要为堆测试数量而增加无关测试。
